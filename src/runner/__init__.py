"""Local Runner — executes commands and Claude CLI in project directories.

Run as a standalone FastAPI service on the developer's Windows PC.
The control plane on Hetzner reaches it via a reverse SSH tunnel.
"""

__version__ = "0.1.0"
