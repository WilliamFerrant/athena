# Claude Code CLI path (defaults to "claude" on PATH)
# CLAUDE_CLI_PATH=claude

# mem0.ai (optional — enables persistent memory)
# MEM0_API_KEY=m0-...

# Model routing (passed to `claude -p --model <model>`)
DEFAULT_MODEL=sonnet
MANAGER_MODEL=sonnet
HEAVY_MODEL=opus

# Daily call limit (rate guard, not cost)
DAILY_CALL_LIMIT=200

# API server
API_HOST=0.0.0.0
API_PORT=8000

# Logging
LOG_LEVEL=INFO

# ── Runner Connector (Control Plane → Local Runner via reverse SSH tunnel) ──
# Base URL where the control plane reaches the runner (through the tunnel)
RUNNER_BASE_URL=http://127.0.0.1:17777
# Shared auth token — must match RUNNER_TOKEN on both sides
# RUNNER_TOKEN=your-secret-token-here
# How often to poll runner /health (seconds)
RUNNER_POLL_INTERVAL=10

# ── Local Runner settings (for companion-runner on Windows) ─────────────────
# Bind address/port for the runner service
RUNNER_HOST=127.0.0.1
RUNNER_PORT=7777
# Auth token (same as RUNNER_TOKEN above)
# RUNNER_TOKEN=your-secret-token-here
# Path to projects.yaml (relative to CWD or absolute)
RUNNER_PROJECTS_FILE=projects.yaml
# Max diff size in bytes (returns 413 if exceeded)
RUNNER_MAX_DIFF_BYTES=500000
