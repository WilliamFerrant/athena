apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: athena-data
  namespace: athena
  labels:
    app.kubernetes.io/name: athena
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uses the default StorageClass — override if needed:
  # storageClassName: hcloud-volumes
